SELECT c.ID,
       c.APP_ID,
       c.LOB_ID,
       c.PROP_NAME
FROM ESIGN_API_DEV.CONFIGURATION c
WHERE EXISTS (
    SELECT 1
    FROM ESIGN_API_DEV.CONFIGURATION d
    WHERE d.APP_ID = c.APP_ID
      AND d.LOB_ID = c.LOB_ID
      AND d.PROP_NAME = c.PROP_NAME
      AND d.ROWID <> c.ROWID
)
ORDER BY c.APP_ID, c.LOB_ID, c.PROP_NAME, c.ID;